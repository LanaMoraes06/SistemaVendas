@model SistemaVendas.Models.Marca

@{
    ViewData["Title"] = "Excluir Marca";
}

<div class="container mt-4">
    <!-- Card com borda vermelha para chamar atenção -->
    <div class="search-card" style="border: 2px solid #ED1C24;">
        <div class="mb-4">
            <h2 class="search-title text-danger">⚠️ Confirmar Exclusão</h2>
            <p class="text-muted">Tem certeza de que deseja excluir permanentemente este registro? Esta ação não pode ser desfeita.</p>
        </div>

        <!-- Exibição dos dados igual à tela de Detalhes -->
        <div class="row mb-4 p-3" style="background-color: #FFF0F0; border-radius: 8px;">
            <div class="col-md-6 mb-3">
                <h6 class="text-danger mb-1" style="font-size: 0.85rem; text-transform: uppercase;">@Html.DisplayNameFor(model => model.Nome)</h6>
                <p class="fs-5 fw-bold text-dark mb-0">@Html.DisplayFor(model => model.Nome)</p>
            </div>
            <div class="col-md-6 mb-3">
                <h6 class="text-danger mb-1" style="font-size: 0.85rem; text-transform: uppercase;">@Html.DisplayNameFor(model => model.Sigla)</h6>
                <p class="fs-5 fw-bold text-dark mb-0">@Html.DisplayFor(model => model.Sigla)</p>
            </div>
        </div>

        <form asp-action="Delete">
            <!-- CAMPO OCULTO PARA CONFIRMAR O ID -->
            <input type="hidden" asp-for="Id" />
            
            <div class="mt-4 pt-3 border-top d-flex gap-2">
                <button type="submit" class="btn btn-danger" style="background-color: #ED1C24; border-radius: 25px; padding: 8px 20px; font-weight: 600; color: white; border: none;">🗑️ Sim, Excluir</button>
                <a asp-action="Index" class="btn btn-light" style="border-radius: 25px; border: 1px solid #ddd;">Cancelar</a>
            </div>
        </form>
    </div>
</div>